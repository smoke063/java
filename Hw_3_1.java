import java.util.List;
import java.util.stream.Collectors;
import java.util.stream.Stream;

class Good {
    public String name;
    public Integer price;
    public String sort;

    static Good [] generate  () {
     /*   List<Good> goods = Stream.iterate(1, n -> new Good())
                .limit(10)
                .collect(Collectors.toList());
        return goods;*/
    }
}

public class Hw_3_1 {
}
